import { RegisterPage } from '@/components/public/register-page'

export const metadata = {
  title: 'Register',
  description: 'Create a student account and explore the demo platform.',
}

export default function RegisterRoute() {
  return <RegisterPage />
}
